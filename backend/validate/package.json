{
  "name": "@rtl-airband-panel/validate",
  "version": "0.3.1",
  "private": true,
  "description": "Semantic validation for RTLSDR-Airband instance configs (frequency windows, FFT bin collisions, CTCSS tones)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@rtl-airband-panel/parser": "*"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^4.1.10"
  }
}
